APP_MODE=demo
APP_ENV=development
POSTGRES_PASSWORD=change-this-local-db-password
MINIO_ROOT_USER=rapiclinics
MINIO_ROOT_PASSWORD=change-this-local-storage-password
DEMO_PASSWORD=RapiDemo2026!
# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
STORAGE_ENCRYPTION_KEY=
CORS_ORIGINS=http://localhost:8081,http://localhost:8082
# Whisper corre en el servidor; descarga inicial de pesos, sin API de pago.
WHISPER_MODEL=small
