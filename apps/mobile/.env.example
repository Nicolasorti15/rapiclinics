# Android emulator: http://10.0.2.2:8000. Web/iOS simulator: http://localhost:8000.
# Physical devices: use the API host on your network or an HTTPS staging server.
EXPO_PUBLIC_API_URL=http://localhost:8000
# APP_IDENTIFIER is required for physical-device/production builds.
# EAS_PROJECT_ID optionally overrides the project already linked in app.config.ts.
# EAS_PROJECT_ID=
# APP_IDENTIFIER=
